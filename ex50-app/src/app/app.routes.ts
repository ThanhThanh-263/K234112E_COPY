import { Routes } from '@angular/router';
import { BookManagementComponent } from './bookmanagementcomponent/bookmanagementcomponent';

export const routes: Routes = [
  { path: '', component: BookManagementComponent }
];
